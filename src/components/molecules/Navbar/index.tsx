import { FC } from "react";
import NavbarProps from "./index.types";

const Navbar: FC<NavbarProps> = ({}) => {
  return <div className="sticky bg-rose-700 h-10 top-0"></div>;
};

export default Navbar;
